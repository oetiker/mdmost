//! Fenced code block highlighting.
//!
//! Placeholder. The interface named in design spec §5 is
//! `highlight(lang, src, &Theme) -> Vec<Line>`. Colours come from
//! [`crate::theme::CodeStyles`], not from a syntect theme, so code sits inside the
//! active palette.
