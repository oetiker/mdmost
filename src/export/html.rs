//! The HTML clipboard flavour: an upgrade offered on top of the TSV, not a replacement.
//!
//! Filled in by Task 6. This stub exists so the module compiles now.

use crate::doc::Node;

/// A table serialised as an HTML `<table>`, escaped and scheme-narrowed.
///
/// Placeholder for Task 6 — always returns an empty string.
pub fn table_html(_node: &Node) -> String {
    String::new()
}
