//! The ratatui application: panes, scrolling, key dispatch, help overlay, status bar.
//!
//! Placeholder. This is the only module allowed to depend on `ratatui` and
//! `crossterm`; it converts [`crate::theme::Style`] into the TUI crate's own style
//! type at the edge.
