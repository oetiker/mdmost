//! Configuration: TOML loading and merging, themes, key bindings.
//!
//! Placeholder. Errors are reported as [`crate::error::ConfigError`] and never prevent
//! startup — the caller falls back to defaults.
